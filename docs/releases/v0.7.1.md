> Registro histórico: este documento descreve o estado exato da `v0.7.1`. Para o comportamento atual, consulte o [índice da documentação](https://github.com/marcelositr/MeliponarioManager/blob/main/docs/README.md).

Primeira pré-release distribuível do MeliponarioManager, consolidando o núcleo de gerenciamento, manejo e rastreabilidade do plantel em uma aplicação desktop local-first.

Esta versão substitui a tentativa de distribuição da tag `v0.7.0`. A aplicação já compilava corretamente, mas o pipeline de bundles revelou que AppImage e MSI exigiam ícones explicitamente declarados na configuração do Tauri. A correção foi feita em `v0.7.1` sem reescrever a tag anterior.

## Destaques

- gerenciamento de múltiplos meliponários, espécies, colônias e caixas;
- histórico de ocupação de caixas sem apagar movimentações anteriores;
- inspeções, alimentação, produção, manutenção e fotos de inspeção;
- eventos e timeline unificada por colônia;
- divisões, multiplicações e genealogia;
- transferências internas, externas e transportes temporários;
- documentos estruturados de movimentação e rastreabilidade;
- ciclo de vida com perda, inativação e reativação;
- alertas derivados diretamente dos dados de manejo;
- dashboard operacional do plantel;
- backup do SQLite e da mídia, exportação JSON, relatório Markdown e restauração preparada com validação de integridade;
- CI endurecido com formatação, Clippy, testes, build desktop e validação dos ícones de bundle;
- pipeline de bundles para Linux e Windows.

## Correção de distribuição em `v0.7.1`

- `bundle.icon` agora declara os PNGs usados no Linux e os formatos `.ico` e `.icns` gerados pelo Tauri;
- AppImage passa a localizar um ícone quadrado válido;
- MSI/WiX passa a localizar o `.ico` exigido pelo instalador Windows;
- CI e pipeline de bundles executam `npm run bundle:check` logo após a geração dos ícones para detectar esse tipo de regressão antes do empacotamento.

## Plataformas e artefatos

O pipeline desta versão está preparado para gerar:

- Linux: `.deb`;
- Linux: AppImage;
- Windows: instalador NSIS;
- Windows: instalador MSI.

Os arquivos devem ser revisados no draft da release antes da publicação.

## Estado da versão

Esta é uma **Pre-release experimental**. A série `0.x` do MeliponarioManager é usada como laboratório contínuo de evolução e testes e não existe meta de chegada a `1.0.0`.

`v0.7.0` permanece preservada como a primeira tag pública e primeira tentativa de distribuição. Ela não resultou em uma GitHub Release concluída por causa das falhas de empacotamento corrigidas nesta versão.

## Dados e rastreabilidade

O aplicativo é local-first. O SQLite permanece no diretório de dados da aplicação e os arquivos de mídia são mantidos separadamente, com metadados registrados no banco.

A modelagem de movimentações considera conceitos encontrados em fluxos de GEFAU, GEDAVE e GTA no Estado de São Paulo, mas o MeliponarioManager não substitui sistemas oficiais, não emite autorizações e não certifica a validade jurídica de documentos.

## Limitações conhecidas

- instaladores Windows ainda não possuem assinatura de código;
- `package-lock.json` e `Cargo.lock` ainda não estão versionados, portanto a resolução exata de dependências ainda não é totalmente reproduzível;
- o projeto ainda precisa de validação mais ampla em uso real e com bases maiores;
- recursos e formatos podem evoluir entre versões `0.x`.

## Antes de testar

Por ser uma versão experimental, mantenha backup dos dados importantes e valide os fluxos de restauração com cópias de trabalho antes de depender exclusivamente deles.

## Documentação

- README: https://github.com/marcelositr/MeliponarioManager/blob/main/README.md
- Modelo de domínio: https://github.com/marcelositr/MeliponarioManager/blob/main/docs/DOMAIN.md
- Gerenciamento de dados: https://github.com/marcelositr/MeliponarioManager/blob/main/docs/DATA-MANAGEMENT.md
- Distribuição: https://github.com/marcelositr/MeliponarioManager/blob/main/docs/DISTRIBUTION.md
- Política de releases: https://github.com/marcelositr/MeliponarioManager/blob/main/docs/RELEASES.md
- Changelog: https://github.com/marcelositr/MeliponarioManager/blob/main/CHANGELOG.md

## Feedback

Problemas reproduzíveis e propostas de melhoria podem ser registrados em GitHub Issues com versão, sistema operacional, passos para reprodução e impacto observado nos dados quando aplicável.
